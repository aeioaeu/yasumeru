# やすめる

**赤ちゃんのパパが育休を取ると世帯のお金がどう動くか**を出す1画面のツール。
素の HTML + CSS + ES モジュール。ビルドもサーバも要らない（`python3 -m http.server`）。

設計の決めごとは `README.md`、色と文字の下限は `design-system/yasumeru/MASTER.md`。

## 必ず守ること

- **画面か文言を変えたら、`personas.md` の5人に通す。**
  `persona-review` スキルの手順で、落ちた項目・直したもの・直さなかったものを分けて残す。
  通っていない変更は出さない。
- **数値を変えるときは、まず手計算をやり直して `test.mjs` の期待値とコメントを直し、
  そのあとで実装を直す。** 逆をやると実装の間違いが期待値に写る。
- **制度の数値は `rules-2026.js` にだけ置く。** `calc.js` に直接書かない。
  出典を開いて確認する。記憶で書き換えない。
- **出せないものは出さない。** `README.md` の「引いた線」を先に読む。
- **CSS か JS を変えたら、`index.html` の `?v=` を上げる。**
  HTML・CSS・JS はクラス名と id で強く結びついているので、片方だけキャッシュに残ると
  画面が壊れる（実際に起きた）。`app.js` は自分の `?v=` を `calc.js` と
  `rules-2026.js` にも引き継ぐので、直すのは `index.html` の2か所だけ。
- 変更したら `node test.mjs` を通す。
